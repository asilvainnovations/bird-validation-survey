{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    /* App-specific overrides — inherits paths and base settings from tsconfig.json */
    "noEmit": true,
    "jsx": "react-jsx"
  },
  "include": ["src"]
}
